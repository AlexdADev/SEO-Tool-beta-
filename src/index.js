import React from 'react';
import ReactDOM from 'react-dom/client';
import PageRankApp from './PageRankApp';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
    <PageRankApp/>
);