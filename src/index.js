import React from 'react';
import ReactDOM from 'react-dom/client';
import PageRankApp from './components/PageRankApp';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
    <PageRankApp/>
);